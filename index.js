import getPosts, { getPostLength } from "./postController.js";

console.log(getPosts(), getPostLength());
// const { generateRandomNumber, celicusToFahrenheit } = require("./utils");

// console.log(
//   `Random Number: ${generateRandomNumber()} and Fahrenheit: ${celicusToFahrenheit(
//     10
//   )}`
// );
